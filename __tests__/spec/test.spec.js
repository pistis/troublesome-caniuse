describe('test for test', () => {
  test('number 1 should be number type.', () => {
    expect(typeof 1).toBe('number')
  })
  test('string "str" should be string type.', () => {
    expect(typeof 'str').toBe('string')
  })
})
